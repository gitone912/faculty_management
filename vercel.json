{
    "builds": [{
        "src": "faculty_management/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "faculty_management/wsgi.py"
        }
    ]
}